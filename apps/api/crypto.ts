const encoder = new TextEncoder();
export function secret() { return Array.from(crypto.getRandomValues(new Uint8Array(32)), x => x.toString(16).padStart(2, '0')).join(''); }
export async function hash(value: string) { return Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256', encoder.encode(value))), x => x.toString(16).padStart(2, '0')).join(''); }
export async function seal(value: string, keyHex: string) { const iv = crypto.getRandomValues(new Uint8Array(12)); const key = await crypto.subtle.importKey('raw', Uint8Array.from(keyHex.match(/../g)!, x => parseInt(x, 16)), 'AES-GCM', false, ['encrypt']); const encrypted = await crypto.subtle.encrypt({ name: 'AES-GCM', iv }, key, encoder.encode(value)); return JSON.stringify({ iv: Array.from(iv), data: Array.from(new Uint8Array(encrypted)) }); }
export async function unseal(value: string, keyHex: string) { const box = JSON.parse(value); const key = await crypto.subtle.importKey('raw', Uint8Array.from(keyHex.match(/../g)!, x => parseInt(x, 16)), 'AES-GCM', false, ['decrypt']); return new TextDecoder().decode(await crypto.subtle.decrypt({ name: 'AES-GCM', iv: new Uint8Array(box.iv) }, key, new Uint8Array(box.data))); }
